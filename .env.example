# Database Configuration (AWS RDS PostgreSQL)
# For local development, use localhost
# For AWS RDS, use your RDS endpoint (e.g., your-db.xxxx.us-east-1.rds.amazonaws.com)
DB_HOST=your-rds-endpoint.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=indic_annotation
DB_USER=postgres
DB_PASSWORD=your_password_here

# Database Connection Options
DB_SSL=true  # Enable SSL for AWS RDS (recommended)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_ECHO=false  # Set to true for SQL debugging

# API Configuration
API_HOST=0.0.0.0
API_PORT=5000
API_DEBUG=false

# Environment
ENVIRONMENT=development

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/pipeline.log

# Data Directories (optional overrides)
# RAW_DATA_DIR=data/raw
# PROCESSED_DATA_DIR=data/processed
# ANNOTATIONS_DIR=data/annotations
# DICTIONARIES_DIR=data/dictionaries

# External API Keys (if needed for data acquisition)
# SANSKRIT_API_KEY=
# TAMIL_API_KEY=
